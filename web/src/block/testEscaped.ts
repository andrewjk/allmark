/*
import type MarkdownNode from "../types/MarkdownNode";
import type ParserState from "../types/ParserState";
import isEscaped from "../utils/isEscaped";

function testEscaped(state: ParserState, _: MarkdownNode) {
	if (isEscaped(state)) {
		state.i++;
		return true;
	}
	return false;
}
*/
